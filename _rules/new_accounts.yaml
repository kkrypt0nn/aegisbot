- rule:
    name: "NewAccounts"
    meta:
      event: "member_join"
      ignoreBots: true
    action:
      type: "alert"
    expression: |
      time.since(message.author.created_at) < duration("168h")
