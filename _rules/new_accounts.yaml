- rule:
    name: "NewAccounts"
    meta:
      event: "member_join"
      ignoreBots: true
    actions:
      - type: "alert"
    expression: |
      time.since(member.created_at) < duration("168h")
