- rule:
    name: "NewAccounts"
    meta:
      event: "member_join"
      ignoreBots: true
    actions:
      - type: "alert"
        channelId: "1215772005681332395"
    expression: |
      time.since(member.created_at) < duration("168h")
